# OmniAgent v8.3 — Python Dependencies
# Install: pip install -r requirements.txt

# Core (required)
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
openai>=1.10.0
slowapi>=0.1.9
pydantic>=2.5.0
cryptography>=42.0.0
starlette>=0.35.0

# Search
ddgs>=6.0.0

# Voice (optional — install for STT/TTS)
faster-whisper>=1.0.0
piper-tts>=1.2.0

# Image Generation (optional)
diffusers>=0.27.0
torch>=2.1.0
transformers>=4.38.0
accelerate>=0.27.0

# RAG & Vector Search
faiss-cpu>=1.8.0
numpy>=1.26.0

# Development
watchfiles>=0.21.0
pytest>=8.0.0
